<h1>For creating new product</h1>
<%= form_for :new_product ,:url => {:action=>"newproduct", :controller=>"static_pages"}  do |f| %>
<% if (@product.errors.any?)%>
  <div id="error_explanation">
     <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>
    <ul>
    <% @product.errors.full_messages.each do |ms| %>
      <li><%= ms %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<!--<h2><%= @id%></h2>-->
 <%= f.hidden_field :hidden, :value => @id %>
Product: <%= f.text_field :product_name %><br>
Product Value: <%= f.text_field :product_value %><br>
 <%= f.submit %>
<% end %>